function PlayerJoinEvent(event){
    var player = event.getPlayer();
    player.sendMessage("Bem-Vindo ao Servidor!");
}

// basta criar uma function
// function BlockBreakEvent(event){}
// e pronto o evento já será executado!